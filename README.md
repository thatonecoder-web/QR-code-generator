# QR-code-generator
A QR code generator built from scratch in Python, implementing encoding, error correction, matrix generation, masking, and rendering without relying on external QR-generation libraries.
